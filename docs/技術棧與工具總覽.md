# 沐寧 Munea · 技術棧與工具總覽（單一總表）

> **這是什麼**：一頁看完「我們用了哪些工具/服務、各自做什麼、目前到哪、花了多少錢」。避免任何人（含 AI）「失憶」重問已決定/已付費的事。
> **維護規矩**：任何人接了新工具、改了狀態、動了預算，**當下就回寫這頁**。動手前也先讀這頁。
> 狀態：✅ 在用（真的在跑）｜🔄 建置中｜📋 已規劃待接｜🧪 PoC 測試中｜⏸️ 暫緩
> 立此因：2026-07-01 Edward 指出「技術棧與已付費工具（RunPod $20）沒有單一紀錄，導致重問」。

---

## 1. 大腦（AI 模型）
| 工具/服務 | 做什麼 | 狀態 | 成本/備註 |
|---|---|---|---|
| **Gemini 2.5 Flash**（+2.0 備援）| 文字聊天回話（寧寧的嘴）| ✅ 在用（`engine/chat_engine.py`，實測會回話、有記憶）| 用量計費；鑰匙 `GEMINI_API_KEY` 已在本機 |
| **Claude Sonnet 5** | 管家腦（背景感知/記憶整理）、守護腦（危機判斷）| 🔄 在用/接入中（`engine/model_router.py`）| 用量計費 |
| **Gemini 3.1 Flash Live** | 即時語音（像打電話、頭號賣點）| 📋 待接（現為 stt-chat-tts 備援）| 即時語音用量計費，最難的一段 |

## 2. 語音
| 工具/服務 | 做什麼 | 狀態 | 成本/備註 |
|---|---|---|---|
| **Voice Provider Adapter** | 語音「可替換層」，之後換 Live 不用重做 | ✅ 在用（baseline）| 自建，`window.MuneaVoiceProvider` |
| 瀏覽器內建語音 / STT→聊天→TTS | 目前的臨時語音（按著說→回話念出來）| ✅ 在用（原型）| 免費/內建，非最終樣子 |

## 3. 會動的臉（Avatar）
| 工具/服務 | 做什麼 | 狀態 | 成本/備註 |
|---|---|---|---|
| static-css / 2d-viseme | 現在的「假動臉」（呼吸＋假嘴型）保底 | ✅ 在用（mock）| 免費，TestFlight 首版可先用 |
| **Ditto**（單卡即時擬真） | 擬真會動的臉·單卡突破口 | 🧪 PoC：**已在 RunPod RTX 4090 測過一輪、單卡可行**，待「線上流暢度(fps)」重測 | 見 `RUNPOD-AVATAR-POC-SCHEDULE.md` |
| **LiveAvatar**（阿里14B / HeyGen 系）| 更高畫質擬真臉 | 📋 待首測（先單張 H100/H200，不燒 5×H800）| 貴，值不值得看 benchmark |
| **RunPod**（租顯卡的地方）| 跑 Ditto/LiveAvatar 的繪圖顯卡機器 | ✅ **已開通、已付 ~US$20 測試預算**、Ditto 已測 | console: console.runpod.io ｜按用量計時 |

## 4. 前端 / App 外殼
| 工具/服務 | 做什麼 | 狀態 | 成本/備註 |
|---|---|---|---|
| **Capacitor + Web Core** | 把網頁包成 iPhone App | ✅ 在用 | 免費開源 |
| 網頁前端（`web/src`, `app.js`）| 5 分頁 UI、聊聊畫面、avatar runtime | ✅ 在用 | — |

## 5. 後端（App 背後的中樞）
| 工具/服務 | 做什麼 | 狀態 | 成本/備註 |
|---|---|---|---|
| Python 引擎（`engine/server.py`）| 送出網頁＋接大腦＋帳務/記憶 | ✅ 在用（本機原型，:8200，多執行緒）| 本機跑；即時語音需要升級成「能長連線」的版本 |
| **正式雲端後端** | 上線後放後端的地方（要能長連線給即時語音）| 📋 待定（見 §7）| 每月小額 |

## 6. 資料庫
| 工具/服務 | 做什麼 | 狀態 | 成本/備註 |
|---|---|---|---|
| **Supabase**（Postgres + 權限規則）| 正式存記憶/帳號/點數/家庭 | 🔄 schema 001-006 已建、可切換 | 有免費額度 |
| 本機 JSON 檔 | 過渡期暫存 | ✅ 在用（過渡，逐步搬去 Supabase）| — |

## 7. 部署 / 上線
| 工具/服務 | 做什麼 | 狀態 | 成本/備註 |
|---|---|---|---|
| **Vercel** | 放網頁前端（官網已在用）| ✅ 官網在用、沐寧規劃用（Edward 確認 OK）| 有免費額度 |
| 小型雲端主機 | 放「即時語音接線」那段（Vercel 不擅長長連線）| 📋 待定：測試先用本機+臨時通道、正式再租 | 每月約 US$5-10 |
| TestFlight / App Store | iPhone 上架給真人測/用 | 📋 未做（需 Mac + Apple 審核 + 麥克風上機）| Apple 開發者 US$99/年 |

## 8. 付費
| 工具/服務 | 做什麼 | 狀態 | 成本/備註 |
|---|---|---|---|
| Apple StoreKit2 / RevenueCat | App 內收訂閱、驗證 | 📋 未接 | RevenueCat 有免費額度 |
| 方案：Free / Plus / Premium / Concierge | 訂閱制；點數只用於加值 | 定案（見 `BILLING-CREDITS-ENTITLEMENT-v1.md`）| — |

## 9. 開發協作
| 工具/服務 | 做什麼 | 狀態 | 成本/備註 |
|---|---|---|---|
| GitHub `EdwardTseng33/Munea` | 程式與文件的家 | ✅ 在用 | — |
| 雙 AI（Claude/城堡 + Codex）+ 協作看板 | 兩個 AI 分工不打架 | ✅ 在用（`協作看板-雙AI分工.md`）| — |

---

## 相關細節文件（深入看這些）
- 架構全貌：`ARCHITECTURE.md`
- 技術棧評估：`TECH-STACK-EVALUATION-2026-06-29.md`
- Avatar/顯卡：`RUNPOD-AVATAR-POC-SCHEDULE.md`、`avatar-顯卡經濟學與LiveAvatar評估-2026-06-27.md`、`Ditto-優化重測指南-2026-06-27.md`
- 語音層：`VOICE-PROVIDER-ADAPTER.md`、`MOBILE-VOICE-BRIDGE.md`
- 後端/資料：`BACKEND-ARCHITECTURE-v1.md`、`supabase/sql/`
- 上架：`APP-STORE-PRODUCTION-READINESS.md`
